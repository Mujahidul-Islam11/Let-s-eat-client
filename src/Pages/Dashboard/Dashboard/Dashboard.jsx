import React from 'react';
import Sidebar from '../SideBar/SideBar';

const Dashboard = () => {
    return (
        <div>
            <Sidebar></Sidebar>
        </div>
    );
};

export default Dashboard;